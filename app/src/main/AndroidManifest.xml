<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flamemathnew">

    <application
        android:allowBackup="true"
        android:icon="@drawable/white_math_ultra_e"
        android:label="@string/app_name"
        android:roundIcon="@drawable/white_math_ultra_e"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlameMathNew">
        <activity android:name=".ui.algebra.MultMatrixActivity" />
        <activity
            android:name=".backend.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.FlameMathNew.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
